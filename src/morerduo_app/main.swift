import SwiftUI

@main
struct MorerduoApp: App {
  var body: some Scene {
    WindowGroup("磨耳朵") {
      Text("磨耳朵")
        .frame(minWidth: 640, minHeight: 560)
    }
    .defaultSize(width: 760, height: 640)
  }
}
