# 验证记录

验证日期：2026-08-01

## 上下文缩减

| 范围 | 文件数 | 字符代理 | Standard 预算结果 |
|---|---:|---:|---|
| 优化前真实 change 样本 | 45 | 285,098 | 超限但继续展开 |
| 优化后同一宽泛 change | 34 | 249,760 | 阻断，并报告 3 个宽泛目录 |
| 工作包 `4.3` | 10 | 20,818 | 通过 |
| Capability `context_routing` | 5 | 48,055 | 通过 |

工作包字符代理较优化前下降约 92.7%，并完整纳入显式声明的 `.mase/gates.yaml` 与 `.mase/tests.yaml`。字符数只是上下文代理，不等同于真实 Token。

## 门禁与测试执行

| 指标 | 优化前 | 优化后 |
|---|---:|---:|
| 最终候选前测试节点执行 | 394 | 204 |
| 重复执行 | 210 | 7 |
| 重复率 | 53.3% | 3.4% |

剩余 7 次来自微循环契约/回滚抽检在后续更大能力集合中的再验证；最终候选全量回归未计入可删除重复。默认 GatePlan 重叠诊断为空，可选 gate 仅由 `--all` 展示。

## 兼容与分发

- `mase update --dry-run` 前后项目文件摘要一致。
- 旧式内嵌完整 evidence 在没有 sidecar 时仍可读取和判定新鲜度。
- 新 evidence 的活动状态只保存紧凑索引；sidecar 丢失或篡改会变为 missing/invalid。
- wheel manifest：82/82 运行时资源一致，无缺失、无额外文件。
- 仓库边界审计通过。

## 回归结果

- 效率变更定向回归：126 passed；随后新增隐藏治理文件回归已纳入完整套件。
- 最终完整 Python 框架回归：225 passed。
- Node/Sandbox 回归：15 passed。
- 两个 OpenSpec change 均通过 `openspec validate`。

影响分析事实源已补齐并通过一致性校验；因当前工作树尚未形成独立提交，实际 diff 复扫和人工 L3 决策仍保持 pending，不伪造 passed evidence。
